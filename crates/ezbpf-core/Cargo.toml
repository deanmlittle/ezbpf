[package]
name = "ezbpf-core"
version = "0.1.0"
edition = "2021"
authors = ["Dean Little <@deanmlittle>"]

[lib]
name = "ezbpf_core"

[dependencies]
anyhow = "1.0"
hex = "0.4"
thiserror = "2.0"
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"

[dev-dependencies]
hex-literal = "1.0"
